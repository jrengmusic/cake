version: 2

project_name: cake

before:
  hooks:
    - go mod tidy

builds:
  - id: cake
    binary: "cake_{{ .Os }}_{{ .Arch }}"
    main: ./cmd/cake
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/jrengmusic/cake/internal.AppVersion={{.Version}}
    hooks:
      post:
        - cmd: bash ./scripts/post-build.sh
          output: true
          env:
            - HOOK_TARGET={{ .Target }}
            - HOOK_PATH={{ .Path }}

archives:
  - formats: [binary]
    name_template: "{{ .Binary }}"

checksum:
  name_template: checksums.txt
  algorithm: sha256

release:
  github:
    owner: jrengmusic
    name: cake
  draft: false
  prerelease: auto
